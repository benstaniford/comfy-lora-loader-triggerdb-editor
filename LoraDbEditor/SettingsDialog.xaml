<Window x:Class="LoraDbEditor.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="550" Width="600"
        Background="{StaticResource BackgroundBrush}"
        Icon="lora-edit.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Paths Section -->
        <TextBlock Grid.Row="0" Text="File Paths" FontWeight="Bold" FontSize="14"
                  Margin="0,0,0,10" Foreground="{StaticResource TextBrush}"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,10"
                  Foreground="{StaticResource TextBrush}" Opacity="0.8">
            Configure the paths to your LoRA database and models folder. Leave as default unless you need custom locations.
        </TextBlock>

        <!-- Database Path -->
        <TextBlock Grid.Row="2" Text="Database Path (lora-triggers.json):"
                  Margin="0,0,0,5" Foreground="{StaticResource TextBrush}"/>

        <Border Grid.Row="3" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
               Padding="10" Margin="0,0,0,15" Background="{StaticResource SurfaceBrush}">
            <TextBox x:Name="DatabasePathTextBox"
                    BorderThickness="0"
                    Background="Transparent"
                    Foreground="{StaticResource TextBrush}"
                    FontFamily="Consolas"
                    FontSize="11"
                    TextChanged="ApiKeyTextBox_TextChanged"/>
        </Border>

        <!-- LoRAs Path -->
        <TextBlock Grid.Row="4" Text="LoRAs Folder Path:"
                  Margin="0,0,0,5" Foreground="{StaticResource TextBrush}"/>

        <Border Grid.Row="5" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
               Padding="10" Margin="0,0,0,20" Background="{StaticResource SurfaceBrush}">
            <TextBox x:Name="LorasPathTextBox"
                    BorderThickness="0"
                    Background="Transparent"
                    Foreground="{StaticResource TextBrush}"
                    FontFamily="Consolas"
                    FontSize="11"
                    TextChanged="ApiKeyTextBox_TextChanged"/>
        </Border>

        <!-- Civitai API Key Section -->
        <TextBlock Grid.Row="6" Text="Civitai API Key" FontWeight="Bold" FontSize="14"
                  Margin="0,0,0,10" Foreground="{StaticResource TextBrush}"/>

        <TextBlock Grid.Row="7" TextWrapping="Wrap" Margin="0,0,0,10"
                  Foreground="{StaticResource TextBrush}" Opacity="0.8">
            Enter your Civitai API key to enable automatic authentication for Civitai downloads.
            Get your API key from: Civitai → Account Settings → API Keys
        </TextBlock>

        <Border Grid.Row="8" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
               Padding="10" Margin="0,0,0,20" Background="{StaticResource SurfaceBrush}">
            <TextBox x:Name="ApiKeyTextBox"
                    BorderThickness="0"
                    Background="Transparent"
                    Foreground="{StaticResource TextBrush}"
                    FontFamily="Consolas"
                    TextChanged="ApiKeyTextBox_TextChanged"/>
        </Border>

        <!-- Status message -->
        <TextBlock Grid.Row="9" x:Name="StatusTextBlock"
                  Foreground="{StaticResource TextBrush}"
                  Opacity="0.6"
                  VerticalAlignment="Top"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save" Width="80" Height="30"
                   Margin="0,0,10,0" Click="SaveButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="30"
                   Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
